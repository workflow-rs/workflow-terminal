[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
workflow-terminal = {path = "../../"}
async-trait = "0.1.57"
workflow-log = { path = "../../../workflow-log" }
